#include <stdio.h>

int main()
{
	for (int i = 5; i >= 1; --i)
		printf("%i ", i);
	return 0;
}


--------------------------------------------------------------------------------------------------------------------------------------------

#include <stdio.h>

void rcs(int s, int e) {
	if (s >= e) {
		printf("%2i", s);
		return rcs(s - 1, e);
	}
}

int main()
{
	rcs(5, 0);
	return 0;
}

--------------------------------------------------------------------------------------------------------------------------------------------

	#include <stdio.h>

int main()
{
	int x = 5;
	while (x >= 0) {
		printf("%2i", x);
		--x;
	}
	return 0;
}

--------------------------------------------------------------------------------------------------------------------------------------------

#include <stdio.h>

int main()
{
	int x = 5;
	do {
		printf("%2i", x);
		--x;
	} while (x >= 0);
	return 0;
}

--------------------------------------------------------------------------------------------------------------------------------------------

#include <stdio.h>

int main()
{
	int x = 5;
pp:
	printf("%2i", x);
	--x;

	if (x >= 0) {
		goto pp;
	}
	return 0;
}


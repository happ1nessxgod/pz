#include <stdio.h>

int main()
{
	for (int i = 1; i <= 5; ++i)
		printf("%i ", i);
	return 0;
}

---------------------------------------------------------------------------------------------------------------------------------------------------------
	
#include <stdio.h>

int main()
{
	int x = 0;
	while (x <= 5) {
		printf("%2i", x);
		++x;
	}
	return 0;
}

---------------------------------------------------------------------------------------------------------------------------------------------------------

#include <stdio.h>

int main()
{
	int x = 0;
	do
	{
		printf("%2i", x);
		++x;
	} while (x <= 5);
	return 0;
}

---------------------------------------------------------------------------------------------------------------------------------------------------------

#include <stdio.h>

int main()
{
	int x = 0;
qq:
	printf("%2i", x);
	x++;

	if (x <= 5)
	{
		goto qq;
	}
return 0;
}

---------------------------------------------------------------------------------------------------------------------------------------------------------

#include <stdio.h>

void rcs(int s, int e) {
	if (s <= e) {
		printf("%2i", s);
		return rcs(s + 1, e);
	}
}

int main()
{
	rcs(0, 5);
	return 0;
}


